# Copy to `.env.local` and adjust Laravel URL.

# URL your Next.js server uses to reach the Laravel API (not the browser).
LARAVEL_API_URL=http://127.0.0.1:8000

# Optional: uppercase currency shown on `/fees/[id]/pay` — must align with Laravel PAYHERE_CURRENCY.
# NEXT_PUBLIC_PAYHERE_CURRENCY=LKR
